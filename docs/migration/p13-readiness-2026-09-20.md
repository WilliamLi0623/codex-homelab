# P13 readiness checkpoint

This checkpoint records the current evidence for the first dedicated-LXC
end-to-end gate. It does not promote unit tests or historical notes to an
end-to-end result.

## Current evidence

| Gate | Status | Evidence and remaining work |
| --- | --- | --- |
| Controller task/attempt persistence | partial | SQLite, REST, MCP, and codexctl paths are implemented and covered by Go tests. |
| Execution broker invariants | partial | Capacity claim, K3s handle persistence, idempotency, and UNKNOWN guards are unit-tested. |
| Production controller wiring | missing | `cmd/controller` still constructs `api.NewServer(database)` without a dispatcher. |
| K3s Job runtime | missing | `internal/executor/k3s` has the lifecycle contract and durable executor, but no production Kubernetes Job adapter. |
| Headless Codex worker | partial | `cmd/agentd` and the App Server stdio client exist with mocked protocol tests; the runtime artifact is not yet installed in the worker template. |
| LXC220 | partial | Last verified evidence shows unprivileged K3s control-plane operation on `local` storage with the control-plane taint. A fresh P13 run still needs live revalidation. |
| LXC3005 | blocked | Rootfs is on `local`, but `pct mount 3005` currently fails at loop setup with `Operation not permitted`; no template write was performed. |
| Local commit E2E | missing | No run has yet produced a task-scoped worktree, validation result, and immutable commit through Controller → LXC → K3s → Codex. |

## 2026-09-20 worker evidence

- LXC3006 `codex-lxc-3006-p13` was cloned from template 3005 with rootfs
  `local:3006/vm-3006-disk-0.raw`, then started as an unprivileged LXC.
- The worker joined LXC220 with K3s `v1.37.0+k3s1` and was observed as
  `Ready`; the live worker address is intentionally not persisted in the public repository.
- Node.js `v20.19.2`, Git `2.43.0`, and Codex CLI `0.155.0` are present.
- `codex-agentd` is installed at `/usr/local/bin/codex-agentd`; the local and
  worker SHA-256 is `e650728a203bef8196126f2f7574d93174630d408b3406434f4ca5310a2356e5`.
- The no-`CODEX_HOME` smoke returned the expected fail-closed error. No
  provider key, `/root/.codex`, or task credential was installed in the worker.
- This is worker-readiness evidence, not P13 completion: a real authenticated
  Controller → K3s Job → Codex turn → validation → local commit and same-task
  follow-up are still pending.
## Safety and routing notes

- The worker template must remain free of CC Hub, OpenAI, GitHub, Proxmox, and
  private SSH credentials.
- `CODEX_HOME` must be attempt-specific and must not be copied from the
  controller or another attempt.
- GLM-5.3 Flash is the replacement for the former MiMo worker role in the
  current routing design. Its last CC Hub probe returned `503
  no_available_providers`, so it remains disabled until a fresh compatibility
  probe succeeds.
- Normal execution remains `dedicated-lxc`; Kueue and destructive migration
  gates remain closed until the plain K3s E2E succeeds.

## Next gate

Implement and test the production dispatcher/runtime wiring with an explicit
fail-closed configuration boundary. Then resolve the LXC3005 loop-mount issue,
install the pinned worker artifacts without credentials, and run the first
isolated Controller-to-local-commit E2E.
